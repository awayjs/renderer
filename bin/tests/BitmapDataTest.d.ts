/// <reference path="../../build/Away3D.next.d.ts" />
declare module tests.display {
    class BitmapDataTest {
        private bitmapData;
        private bitmapDataB;
        private imgLoader;
        private urlRequest;
        constructor();
        private onMouseDown(e);
        private imgLoadedError(e);
        private imgLoaded(e);
    }
}
