/// <reference path="../../build/Away3D.next.d.ts" />
declare module demos.cubes {
    class CubeDemoTest {
        private _view;
        private _cube;
        private _mesh;
        private raf;
        constructor();
        private render(dt?);
        public resize(e): void;
    }
}
