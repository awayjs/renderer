import { Rectangle } from "@awayjs/core/lib/geom/Rectangle";
import { EventDispatcher } from "@awayjs/core/lib/events/EventDispatcher";
import { Stage } from "@awayjs/stage/lib/base/Stage";
import { IIndexBuffer } from "@awayjs/stage/lib/base/IIndexBuffer";
import { IVertexBuffer } from "@awayjs/stage/lib/base/IVertexBuffer";
export declare class RTTBufferManager extends EventDispatcher {
    private static _instances;
    private _renderToTextureVertexBuffer;
    private _renderToScreenVertexBuffer;
    private _indexBuffer;
    private _stage;
    private _viewWidth;
    private _viewHeight;
    private _textureWidth;
    private _textureHeight;
    private _renderToTextureRect;
    private _buffersInvalid;
    private _textureRatioX;
    private _textureRatioY;
    constructor(stage: Stage);
    static getInstance(stage: Stage): RTTBufferManager;
    private static getRTTBufferManagerFromStage(stage);
    private static deleteRTTBufferManager(stage);
    readonly textureRatioX: number;
    readonly textureRatioY: number;
    viewWidth: number;
    viewHeight: number;
    readonly renderToTextureVertexBuffer: IVertexBuffer;
    readonly renderToScreenVertexBuffer: IVertexBuffer;
    readonly indexBuffer: IIndexBuffer;
    readonly renderToTextureRect: Rectangle;
    readonly textureWidth: number;
    readonly textureHeight: number;
    dispose(): void;
    private updateRTTBuffers();
}
