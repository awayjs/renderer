/// <reference path="../../build/Away3D.next.d.ts" />
declare module tests.display {
    class BitmapDataReflectionTest {
        private view;
        private raf;
        private reflectionMesh;
        constructor();
        private onResourceComplete(e);
        private resize();
        private render();
    }
}
